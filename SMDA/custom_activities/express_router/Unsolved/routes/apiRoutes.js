var express = require('express');
var router = express.Router();
var db = require('../db/data.js');

//write a get route for retrieving the data, and a post route for adding new data
//the api routes should hit the route: /api/cards
//db is just an array; to add something to it just .push() the data into it
//YOUR CODE GOES HERE...

module.exports = router;